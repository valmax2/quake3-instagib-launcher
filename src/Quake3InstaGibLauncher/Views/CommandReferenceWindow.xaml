<Window x:Class="Quake3InstaGibLauncher.Views.CommandReferenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Quake3InstaGibLauncher.ViewModels"
        mc:Ignorable="d"
        Title="{DynamicResource Str.KeyBindings.RefWindowTitle}"
        Width="640" Height="720" MinWidth="480" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.Bg1}">

    <Window.Resources>
        <DataTemplate x:Key="RefRowTemplate" DataType="{x:Type vm:CommandReferenceItem}">
            <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                    <TextBlock Text="{Binding Description}" Style="{StaticResource Text.Body}" FontSize="12" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Command}" FontFamily="{StaticResource Font.Mono}" FontSize="11" Foreground="{StaticResource Brush.TextDim}" TextWrapping="Wrap"/>
                </StackPanel>
                <Button Grid.Column="1" Content="📋" Style="{StaticResource Btn.Icon}" VerticalAlignment="Center"
                        ToolTip="{DynamicResource Str.KeyBindings.RefCopyTooltip}"
                        Tag="{Binding}" Click="CopyButton_Click"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="{DynamicResource Str.KeyBindings.RefWindowTitle}" Style="{StaticResource Text.Title}" FontSize="22" Margin="0,0,0,4"/>
            <TextBlock Style="{StaticResource Text.Muted}" TextWrapping="Wrap" Text="{DynamicResource Str.KeyBindings.RefWindowInfo}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Style="{StaticResource Panel.Card}" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Str.KeyBindings.ConsoleHeader}" Style="{StaticResource Text.Section}" Margin="0,0,0,8"/>
                        <ItemsControl ItemsSource="{Binding ConsoleCommands}" ItemTemplate="{StaticResource RefRowTemplate}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Panel.Card}" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Str.KeyBindings.ChatHeader}" Style="{StaticResource Text.Section}" Margin="0,0,0,8"/>
                        <ItemsControl ItemsSource="{Binding ChatPhrases}" ItemTemplate="{StaticResource RefRowTemplate}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2" Content="{DynamicResource Str.KeyBindings.RefClose}" Style="{StaticResource Btn.Primary}"
                HorizontalAlignment="Right" Margin="0,14,0,0" Click="CloseButton_Click"/>
    </Grid>
</Window>
