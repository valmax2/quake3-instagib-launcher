<UserControl x:Class="Quake3InstaGibLauncher.Views.PlayerProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="900">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40,28" MaxWidth="820">
            <TextBlock Text="{DynamicResource Str.PlayerProfile.Title}" Style="{StaticResource Text.Title}" FontSize="28" Margin="0,0,0,6"/>
            <TextBlock Style="{StaticResource Text.Muted}" TextWrapping="Wrap" Margin="0,0,0,20"
                       Text="{DynamicResource Str.PlayerProfile.Intro}"/>

            <!-- ===================== Nome colorato ===================== -->
            <Border Style="{StaticResource Panel.Card}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Str.PlayerProfile.NameHeader}" Style="{StaticResource Text.Section}" Margin="0,0,0,10"/>
                    <TextBlock Style="{StaticResource Text.Muted}" TextWrapping="Wrap" Margin="0,0,0,10"
                               Text="{DynamicResource Str.PlayerProfile.NameInfo}"/>
                    <TextBox x:Name="NameTextBox" Text="{Binding ColoredName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource Input.TextBox}" FontFamily="{StaticResource Font.Mono}" Margin="0,0,0,10"/>

                    <TextBlock Text="{DynamicResource Str.PlayerProfile.PaletteHeader}" Style="{StaticResource Text.Muted}" Margin="0,0,0,6"/>
                    <StackPanel x:Name="PalettePanel" Orientation="Horizontal" Margin="0,0,0,14"/>

                    <TextBlock Text="{DynamicResource Str.Common.PreviewLabel}" Style="{StaticResource Text.Muted}" Margin="0,0,0,4"/>
                    <Border Background="{StaticResource Brush.Bg0}" BorderBrush="{StaticResource Brush.PanelBorder}" BorderThickness="1" CornerRadius="3" Padding="12,10" Margin="0,0,0,10">
                        <TextBlock x:Name="PreviewText" FontFamily="{StaticResource Font.Display}" FontSize="18"/>
                    </Border>

                    <Button Content="{DynamicResource Str.PlayerProfile.ResetName}" Style="{StaticResource Btn.Base}" HorizontalAlignment="Left" Command="{Binding ResetNameCommand}"/>
                </StackPanel>
            </Border>

            <!-- ===================== Mirino ===================== -->
            <Border Style="{StaticResource Panel.Card}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Str.PlayerProfile.CrosshairHeader}" Style="{StaticResource Text.Section}" Margin="0,0,0,10"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="{DynamicResource Str.PlayerProfile.CrosshairStyle}" Style="{StaticResource Text.Muted}"/>
                            <ComboBox ItemsSource="{Binding AvailableCrosshairStyles}" SelectedItem="{Binding CrosshairStyle}" Style="{StaticResource Input.ComboBox}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="{DynamicResource Str.PlayerProfile.CrosshairColor}" Style="{StaticResource Text.Muted}"/>
                            <TextBox Text="{Binding CrosshairColorHex, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Input.TextBox}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="{Binding CrosshairSize, StringFormat={StaticResource Str.PlayerProfile.CrosshairSizeFormat}}" Style="{StaticResource Text.Muted}" Margin="0,0,0,2"/>
                    <Slider Minimum="4" Maximum="96" Value="{Binding CrosshairSize}" Style="{StaticResource Input.Slider}" TickFrequency="4" Margin="0,0,0,10"/>

                    <CheckBox Content="{DynamicResource Str.PlayerProfile.CrosshairHealthColor}" IsChecked="{Binding CrosshairHealthColor}" Margin="0,0,0,6"/>
                    <CheckBox Content="{DynamicResource Str.PlayerProfile.CrosshairPulse}" IsChecked="{Binding CrosshairPulseOnHit}"/>
                </StackPanel>
            </Border>

            <!-- ===================== Colori modello ===================== -->
            <Border Style="{StaticResource Panel.Card}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Str.PlayerProfile.ModelColorsHeader}" Style="{StaticResource Text.Section}" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="{DynamicResource Str.PlayerProfile.PrimaryColor}" Style="{StaticResource Text.Muted}"/>
                            <ComboBox ItemsSource="{Binding AvailableModelColors}" SelectedValuePath="Value"
                                      SelectedValue="{Binding ModelColor1}" Style="{StaticResource Input.ComboBox}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="{DynamicResource Str.PlayerProfile.SecondaryColor}" Style="{StaticResource Text.Muted}"/>
                            <ComboBox ItemsSource="{Binding AvailableModelColors}" SelectedValuePath="Value"
                                      SelectedValue="{Binding ModelColor2}" Style="{StaticResource Input.ComboBox}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Text.Muted}" TextWrapping="Wrap"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
